# Changesets

This directory contains release notes and version bump intents for `@rapidset/rapidkit`.

Create a changeset when a PR changes package behavior:

```bash
pnpm changeset
```

Then commit the generated markdown file in this folder.
