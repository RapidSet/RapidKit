import { useEffect } from 'react';

function LandingTopNavSpy() {
useEffect(() => {
const links = Array.from(
document.querySelectorAll('.mz-topnav-link[data-section]')
);

    if (links.length === 0) {
      return;
    }

    const sectionIds = links
      .map((link) => link.getAttribute('data-section'))
      .filter(Boolean);

    const sections = sectionIds
      .map((id) => document.getElementById(id))
      .filter(Boolean);

    if (sections.length === 0) {
      return;
    }

    const setActive = (id) => {
      links.forEach((link) => {
        const isMatch = link.getAttribute('data-section') === id;
        link.classList.toggle('is-active', isMatch);
        link.setAttribute('aria-current', isMatch ? 'true' : 'false');
      });
    };

    const observer = new IntersectionObserver(
      (entries) => {
        const visible = entries
          .filter((entry) => entry.isIntersecting)
          .sort((a, b) => b.intersectionRatio - a.intersectionRatio);

        if (visible.length > 0) {
          setActive(visible[0].target.id);
        }
      },
      {
        rootMargin: '-30% 0px -55% 0px',
        threshold: [0.2, 0.35, 0.5, 0.7],
      }
    );

    sections.forEach((section) => observer.observe(section));
    setActive(sections[0].id);

    return () => {
      observer.disconnect();
    };

}, []);

return null;
}

<div className="mz-home">
  <LandingTopNavSpy />
  <header className="mz-topbar" aria-label="Landing navigation">
    <a className="mz-logo" href="/">
      <span className="mz-logo-dot" aria-hidden="true" />
      <span>mezmer</span>
    </a>
    <nav className="mz-topnav">
      <a className="mz-topnav-link" data-section="features" href="#features">Features</a>
      <a className="mz-topnav-link" data-section="components" href="#components">Components</a>
      <a className="mz-topnav-link" data-section="install" href="#install">Install</a>
      <a href="/THEMING">Themes</a>
      <a href="/MCP-SERVER">MCP</a>
    </nav>
    <div className="mz-topbar-cta">
      <a className="mz-btn mz-btn-ghost" href="/components/">Docs</a>
      <a className="mz-btn mz-btn-primary" href="/INSTALLATION">Get Started</a>
    </div>
  </header>

  <section className="mz-hero">
    <div className="mz-hero-grid">
      <div>
        <p className="mz-eyebrow">Ship Confident UI</p>
        <h1>Production-grade React components with AI-first docs and contracts.</h1>
        <p className="mz-subtitle">
          Mezmer combines reusable components, strict typed APIs, and
          machine-readable contracts so teams can design, implement, and ship
          faster without coupling to product-specific logic.
        </p>
        <div className="mz-hero-actions">
          <a className="mz-btn mz-btn-primary" href="/components/">
            Explore Components
          </a>
          <a className="mz-btn mz-btn-ghost" href="/INSTALLATION">
            Install Package
          </a>
        </div>
      </div>
      <aside className="mz-hero-panel" aria-label="Package preview">
        <div className="mz-panel-head">
          <span className="mz-panel-dot" aria-hidden="true" />
          <span>@tarikukebede/mezmer</span>
        </div>
        <pre className="mz-panel-code">
          <code>{`import { BaseTable, DatePicker, Search } from '@tarikukebede/mezmer';

export function RevenueView() {
  return (
    <div>
      <Search placeholder="Search customers" />
      <DatePicker />
      <BaseTable />
    </div>
  );
}`}</code>
        </pre>
        <ul className="mz-panel-points">
          <li>Strict TypeScript public APIs</li>
          <li>Accessibility-first interaction defaults</li>
          <li>Contract-driven docs and tests</li>
        </ul>
      </aside>
    </div>
    <div className="mz-stats" aria-label="Mezmer highlights">
      <article>
        <p className="mz-stat-value">14+</p>
        <p className="mz-stat-label">Core Components</p>
      </article>
      <article>
        <p className="mz-stat-value">AI Contracts</p>
        <p className="mz-stat-label">Build + Docs Aligned</p>
      </article>
      <article>
        <p className="mz-stat-value">TS Strict</p>
        <p className="mz-stat-label">Typed Public API</p>
      </article>
    </div>
  </section>

<section className="mz-trust" aria-label="Technology stack highlights">
  <p>Built on proven foundations</p>
  <div className="mz-trust-row">
    <span>Radix UI</span>
    <span>shadcn/ui</span>
    <span>Tailwind CSS</span>
    <span>Zod</span>
    <span>TanStack</span>
    <span>RTK Query</span>
  </div>
</section>

<section id="features" className="mz-section">
  <h2>Built for modern product teams</h2>
  <div className="mz-grid mz-grid-3">
    <article className="mz-card">
      <h3>Composable By Design</h3>
      <p>
        Presentational, data-source agnostic components that stay portable
        across apps and frameworks.
      </p>
    </article>
    <article className="mz-card">
      <h3>Contract-Driven Delivery</h3>
      <p>
        Keep implementation, tests, and docs synchronized through structured
        contracts under AI-ready workflows.
      </p>
    </article>
    <article className="mz-card">
      <h3>Accessible Defaults</h3>
      <p>
        Keyboard support, semantic roles, and interaction guarantees validated
        with Vitest and targeted browser confidence tests.
      </p>
    </article>
  </div>
</section>

<section id="components" className="mz-section">
  <h2>Component surfaces that scale</h2>
  <div className="mz-grid mz-grid-4">
    <a className="mz-pill" href="/components/base-table">
      BaseTable
    </a>
    <a className="mz-pill" href="/components/base-modal">
      BaseModal
    </a>
    <a className="mz-pill" href="/components/autocomplete">
      Autocomplete
    </a>
    <a className="mz-pill" href="/components/date-picker">
      DatePicker
    </a>
    <a className="mz-pill" href="/components/input">
      Input
    </a>
    <a className="mz-pill" href="/components/search">
      Search
    </a>
    <a className="mz-pill" href="/components/drop-down">
      DropDown
    </a>
    <a className="mz-pill" href="/components/button">
      Button
    </a>
  </div>
</section>

<section id="install" className="mz-section mz-install">
  <div>
    <h2>Install in seconds</h2>
    <p>
      Add Mezmer and start composing polished interfaces with a package-first,
      tree-shakeable architecture.
    </p>
  </div>
  <pre>
    <code>pnpm add @tarikukebede/mezmer</code>
  </pre>
</section>

  <section className="mz-cta">
    <h2>Design faster. Ship cleaner.</h2>
    <p>
      Browse the docs, explore themes, and integrate components into your
      workflow with confidence.
    </p>
    <div className="mz-hero-actions">
      <a className="mz-btn mz-btn-primary" href="/components/">Browse Documentation</a>
      <a className="mz-btn mz-btn-ghost" href="/THEMING">View Themes</a>
    </div>
  </section>
</div>
