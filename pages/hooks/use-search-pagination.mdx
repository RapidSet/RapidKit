import { HookExampleTabs } from '../../docs-nextra/components/HookExampleTabs';

# useSearchPagination

`useSearchPagination` provides a small, reusable state model for search + pagination list experiences.

## What It Handles

- current `PaginationParams` state
- query updates through `handleSearch`
- page reset to `1` on every new search
- full param replacement through `handlePaginationChange`

## Usage

The example below includes both a live preview and full code.

<div className="component-example-tabs-host">
  <HookExampleTabs hook="use-search-pagination" />
</div>
